{
  "name": "stockroom",
  "version": "1.0.0",
  "description": "Inventory tracker with reminders, vendors, and product links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
